<h5><%= link_to comment.user.full_name, user_path(comment.user_id) %></h5>
<p><%= comment.body %></p>
<% if comment.user == current_user %>
  <%= link_to 'Edit', edit_comment_path(comment) %>
  <%= link_to 'Delete', comment, method: :delete, data: { confirm: 'Are you sure?' } %>
<% end %>
<br>
