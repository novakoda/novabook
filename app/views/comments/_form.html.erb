<%= simple_form_for(@comment) do |f| %>
  <%= f.hidden_field :user_id, value: current_user.id  %>
  <%= f.hidden_field :post_id, value: @post.id  %>
  <%= f.input :body, placeholder: "Make a comment" %>
  <%= f.button :submit %>
<% end %>
